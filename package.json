{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter=\"ai-chat-bot\" dev",
    "build": "pnpm --filter=\"ai-chat-bot\" build",
    "start": "pnpm --filter=\"ai-chat-bot\" start",
    "prod": "pnpm run build && pnpm run start"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
